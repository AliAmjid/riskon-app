{
  "name": "riskon-app",
  "version": "0.1.0",
  "private": true,
  "description": "Riskon web app — trigger OR agent runs via Cursor SDK",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run start:dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w @riskon/shared && npm run build -w api && npm run build -w web",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
